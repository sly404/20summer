<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.manager.login.Mapper.UserMapper">
    <resultMap id="result_user" type="com.manager.login.Entity.User">
     <result column="user_ID" jdbcType="INTEGER" property="user_ID"/>
        <result column="user_name" jdbcType="VARCHAR" property="use_name"/>
        <result column="password" jdbcType="VARCHAR" property="user_password"/>
        <result column="phone" jdbcType="VARCHAR" property="user_phone"/>
        <result column="sex" jdbcType="VARCHAR" property="user_sex"/>
        <result column="birthday" jdbcType="VARCHAR" property="user_birthday"/>
        <result column="build_time" jdbcType="VARCHAR" property="user_buildtime"/>
        <result column="state" jdbcType="INTEGER" property="user_state"/>
        <result column="note" jdbcType="VARCHAR" property="user_note"/>
    </resultMap>
    <select id="SelectByName" resultMap="result_user">
        SELECT * FROM user where user_name = #{user_name};
    </select>
    <select id="SelectByID" resultMap="result_user">
        SELECT * FROM user where user_id = #{user_id};
    </select>
    <insert id="insert" parameterType="com.manager.login.Entity.User">
        insert into user (
        user_ID,user_name,password,phone,sex,birthday,build_time,state,note
        )
        VALUES (
        #{user_id},#{user_name},#{user_password},#{user_phone},#{user_sex},#{user_birthday},#{user_buildtime},#{user_state},#{user_note}
        );
    </insert>
    <update id="update" parameterType="com.manager.login.Entity.User">
      UPDATE user SET password=#{user_password},phone=#{user_phone},sex=#{user_sex},birthday=#{user_birthday},
        state=#{user_state},note=#{user_note} WHERE user_ID=#{user_id};
    </update>
</mapper>
